{
  "name": "typescript-reboot",
  "version": "1.0.0",
  "description": "makes a network request to fetch some json and print the result",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eunsang",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0"
  }
}
