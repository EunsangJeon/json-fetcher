// type alias
type Drink = [string, boolean, number];

const pepsi: Drink = ['brown', true, 40];
const sprite: Drink = ['clear', true, 40];
const greenTea: Drink = ['green', false, 0];

// frankly I've never seen tuple in real project yet..
